-- Create schema for card related tables if it doesn't exist
-- PostgreSQL schema creation syntax

CREATE SCHEMA IF NOT EXISTS cards;

COMMENT ON SCHEMA cards IS 'Schema for card related tables and objects';